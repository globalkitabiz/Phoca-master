# ===========================================
# Phoca Backend 환경 변수 설정
# ===========================================
# 이 파일을 .dev.env로 복사한 후 실제 값을 입력하세요
# cp .env.example .dev.env

# -------------------------------------------
# Database (Supabase PostgreSQL)
# -------------------------------------------
# Supabase 대시보드 > Settings > Database에서 확인
DB_HOST=db.xxxxxxxxxxxx.supabase.co
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your-supabase-password
DB_NAME=postgres

# -------------------------------------------
# JWT 인증
# -------------------------------------------
# 임의의 긴 문자열 (예: openssl rand -hex 32)
JWT_SECRET_KEY=your-super-secret-jwt-key-minimum-32-characters
JWT_EXPIRESIN=7d

# -------------------------------------------
# Cloudflare R2 (이미지 저장소)
# -------------------------------------------
# Cloudflare 대시보드 > R2 > Manage R2 API Tokens에서 생성
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
R2_BUCKET_NAME=phoca-images
R2_PUBLIC_URL=https://pub-xxxxx.r2.dev

# -------------------------------------------
# Replicate AI (객체 인식)
# -------------------------------------------
# https://replicate.com/account/api-tokens에서 생성
REPLICATE_API_TOKEN=r8_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -------------------------------------------
# Google Cloud Translate (번역)
# -------------------------------------------
# Google Cloud Console > APIs & Services > Credentials
# 서비스 계정 JSON에서 추출
client_email=your-service-account@project-id.iam.gserviceaccount.com
private_key="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"
projectId=your-google-cloud-project-id

# -------------------------------------------
# Email (비밀번호 찾기용)
# -------------------------------------------
# Gmail 사용 시: Google 계정 > 보안 > 앱 비밀번호 생성
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# -------------------------------------------
# Kakao OAuth (선택사항)
# -------------------------------------------
# https://developers.kakao.com에서 앱 생성 후 확인
KAKAO_CLIENT_ID=your-kakao-rest-api-key
KAKAO_CALLBACK_URL=https://your-backend-url.railway.app/auth/kakao/callback
